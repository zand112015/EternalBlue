#include <iostream>
#include <stdio.h>

// TODO rando ( rise -> port -> connect ) 

USHORT __rando() {
  USHORT rando;
  for (rando = 0; rando > 0; rando++) {
    while (rando++) {
      return rando;
    }
  }
}
